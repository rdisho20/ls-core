/*
interface User {
  name: string;
  age: number;
  email?: string;
  readonly id: number;
}

type User = {
  name: string;
  age: number;
  email?: string;
  readonly id: number;
}

type myString = string;
type customBoolean = boolean;
type DogAndCat = "dog" | " cat";
type Vehicle = 
  | { kind: "car"; wheels: number }
  | { kind: "plane"; engines: number };

interface User {
  name: string;
}

interface User {
  age: number;
}

const user: User = {
  name: "John",
  age: 30,
}
*/

/*
interface Animal {
  name: string;
  makeSound(): void;
}

interface Dog extends Animal {
  breed: string;
}

type Dog2 = Animal & { breed: string };
*/

